---
name: Go

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  go:
    uses: openconfig/common-ci/.github/workflows/go.yml@v0.2.0
    with:
      coverage-excludes-regex: /cloudbuild
      tests-excludes-regex: /cloudbuild
      race-tests-excludes-regex: /cloudbuild
      skip-race-tests: true
      go-versions: "['1.21']"

  linter:
    uses: openconfig/common-ci/.github/workflows/linter.yml@v0.2.0

  runner-specs:
    name: Runner Specs and Disk Utilization
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Log hardware specifications and disk usage
        run: |
          echo "=== CPU Info ==="
          nproc
          lscpu | grep -E "Model name|CPU\(s\):|Thread\(s\) per core|Core\(s\) per socket"
          echo "=== Memory Info ==="
          free -h
          echo "=== Disk Info ==="
          df -h
